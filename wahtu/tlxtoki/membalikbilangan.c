#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void)
{
    unsigned int n, x[10000];
    scanf("%lu", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%lu", x[i]);
    }

    return 0;
}